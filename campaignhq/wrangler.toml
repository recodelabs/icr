# Campaign Dashboards on Cloudflare: a Worker that serves the Framework build from the
# `icr` R2 bucket at https://monitor.healthcampaigns.org.
# Deploy with ./deploy.sh (build → sync dist/ to R2 → wrangler deploy).
name = "campaignhq"
main = "worker/index.js"
compatibility_date = "2026-09-01"
account_id = "af8ac7eb1f7ec617e14702e69ce0d4c7"
workers_dev = false

[[r2_buckets]]
binding = "ICR"
bucket_name = "icr"

# Custom domain on the healthcampaigns.org zone (same account). Wrangler creates
# the DNS record and certificate for a custom_domain route on deploy.
[[routes]]
pattern = "monitor.healthcampaigns.org"
custom_domain = true
