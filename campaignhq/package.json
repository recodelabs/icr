{
  "name": "campaignhq",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CampaignHQ — the ICR campaign calendar dashboard (Observable Framework, DuckDB-WASM, MapLibre)",
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "dev": "observable preview",
    "build": "observable build",
    "deploy": "observable deploy"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
